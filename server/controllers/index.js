module.exports = { // exporting the file as a module... exporting as an object
    userController: require("./userController"),
    journalController: require("./journalcontroller"), // the property is journalController and the value of the property is the import of the journalcontroller file
};